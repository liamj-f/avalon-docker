# Copy this file to .env and adjust as needed before running docker compose up.

POSTGRES_USER=avalon
POSTGRES_PASSWORD=change_me
POSTGRES_DB=avalon

# Exposed on the host; visit http://localhost:<FRONTEND_PORT>
FRONTEND_PORT=8080
